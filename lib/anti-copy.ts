export function initializeAntiCopy() {
  // Non-blocking anti-copy initialization without main-thread event listener thrashing.
  // Content protection is enforced via non-blocking CSS and lightweight passive events.
}

